import { describe, expect, it } from "vitest";
import AddRestaurant from "../pages/AddRestaurant";

describe("AddRestaurant", () => {
  it('should be a function', () => {
    expect(typeof AddRestaurant).toBe('function')
  })

  it('should return a restaurant', () => {
    
  })

})